window.__COSMIC_V6_CODE=window.__COSMIC_V6_CODE||[];window.__COSMIC_V6_CODE.push(" button');bs.forEach(b=>b.disabled=true);bs[c]?.classList.add('correct');if(i===c){state.rescues++;state.score+=220;if(state.rescueType==='fuel')state.fuel=70;else state.hull=75;$('#rescueFeedback').textContent='✓ ถูกต้อง! ระบบกู้ยานทำงาน';sfx('rescue');setTimeout(()=>{$('#rescueOverlay').classList.add('hidden');state.paused=false;state.running=true;player.inv=2.6;state.last=performance.now();state.raf=requestAnimationFrame(loop)},800)}else{btn.classList.add('wrong');state.score=Math.max(0,state.score-40);$('#rescueFeedback').textContent='ยังไม่ถูก เปลี่ยนคำถามแล้วลองใหม่';sfx('fail');setTimeout(()=>{state.qIndex=(state.qIndex+1)%sectors[state.sector].questions.length;renderQuestion()},700)}}\nfunction finishRun(success=false){state.running=false;cancelAnimationFrame(state.raf);saveLeader();show('report');$('#reportTitle').textContent=success?'GALAXY CLEARED!':'EXPEDITION REPORT';$('#reportScore').textContent=state.score.toLocaleString('th-TH');$('#reportSectors').textContent=state.sectors+(success?1:0);$('#reportKills').textContent=state.totalKills;$('#reportBosses').textContent=state.bosses;$('#reportRescues').textContent=state.rescues;$('#reportCombo').textContent='x'+state.bestCombo;$('#reportPowers').textContent=state.pickupCount;sfx('clear')}\nfunction showPickup(t){const e=$('#pickupToast');e.textContent=t;e.classList.add('show');clearTimeout(e._t);e._t=setTimeout(()=>e.classList.remove('show'),1200)}\nfunction dist(a,b){return Math.hypot(a.x-b.x,a.y-b.y)}function burst(x,y,type,n){for(let i=0;i<n;i++)state.particles.push({x,y,vx:(Math.random()-.5)*320,vy:(Math.random()-.5)*320,life:.45+Math.random()*.5,max:.95,r:1+Math.random()*4,type,spin:Math.random()*Math.PI*2});if(['boom','bomb','hit','power','collect'].includes(type)){const grow={boom:140,bomb:220,hit:90,power:120,collect:90}[type]||100;state.shocks.push({x,y,r:6,life:type==='bomb'?.65:.35,max:type==='bomb'?.65:.35,grow,color:{boom:'#ff9f5a',bomb:'#ff6178',hit:'#fff06a',power:'#7bf0ff',collect:'#9cffc2'}[type]||'#fff'})}if(state.particles.length>180)state.particles.splice(0,state.particles.length-180);if(state.shocks.length>18)state.shocks.splice(0,state.shocks.length-18)}\nfunction updateParticles(dt){state.particles.forEach(p=>{p.x+=p.vx*dt;p.y+=p.vy*dt;p.vx*=.98;p.vy*=.98;p.life-=dt});state.particles=state.particles.filter(p=>p.life>0);state.shocks.forEach(s=>{s.life-=dt;s.r+=s.grow*dt});state.shocks=state.shocks.filter(s=>s.life>0)}\nfunction loop(t){if(!state.running)return;const dt=Math.min(.033,(t-state.last)/1000||0);state.last=t;update(dt);draw();if(state.running)state.raf=requestAnimationFrame(loop)}\nfunction resize(){const r=arena.getBoundingClientRect();DPR=Math.min(devicePixelRatio||1,2);W=Math.max(1,r.width);H=Math.max(1,r.height);canvas.width=Math.round(W*DPR);canvas.height=Math.round(H*DPR);ctx.setTransform(DPR,0,0,DPR,0,0);state.stars=Array.from({length:Math.min(170,Math.max(100,Math.floor(W*H/4800)))},()=>({x:Math.random()*W,y:Math.random()*H,v:30+Math.random()*105,s:.5+Math.random()*1.8}));if(!player.x){player.x=W/2;player.y=H*.7}draw()}\nfunction draw(){const bg=currentBg(),g=ctx.createLinearGradient(0,0,0,H);g.addColorStop(0,bg[0]);g.addColorStop(1,bg[1]);ctx.fillStyle=g;ctx.fillRect(0,0,W,H);drawBackdrop();drawStars();drawShockwaves();drawCores();drawPickups();drawEnemies();drawBoss();drawBullets();drawParticles();drawPlayer();if(state.laserT>0)drawLaser()}\nfunction drawBackdrop(){const sec=sectors[state.sector];const pos={earth:[W*.84,H*.14,210],moon:[W*.84,H*.14,195],mars:[W*.84,H*.15,210],hole:[W*.82,H*.18,250]}[sec.id];if(pos&&drawPlanetAsset(sec.id,pos[0],pos[1],pos[2]))return;if(sec.id==='earth')planet(W*.84,H*.14,95,'#73dcff','#2469bd','#102d65');if(sec.id==='moon')planet(W*.84,H*.14,82,'#f1f1ed','#a6abb4','#555c67');if(sec.id==='mars')planet(W*.84,H*.15,98,'#ffc092','#bd5734','#512013');if(sec.id==='hole'){ctx.save();ctx.translate(W*.82,H*.18);ctx.strokeStyle='#ffad5c';ctx.lineWidth=13;ctx.beginPath();ctx.ellipse(0,0,120,35,-.18,0,Math.PI*2);ctx.stroke();ctx.strokeStyle='#b36cff';ctx.lineWidth=7;ctx.beginPath();ctx.ellipse(0,0,145,43,-.18,0,Math.PI*2);ctx.stroke();ctx.fillStyle='#000';ctx.beginPath();ctx.arc(0,0,48,0,Math.PI*2);ctx.fill();ctx.restore()}}\nfunction planet(x,y,r,a,b,c){const g=ctx.createRadialGradient(x-r*.3,y-r*.3,4,x,y,r);g.addColorStop(0,a);g.addColorStop(.45,b);g.addColorStop(1,c);ctx.fillStyle=g;ctx.beginPath();ctx.arc(x,y,r,0,Math.PI*2);ctx.fill();ctx.strokeStyle='#ffffff22';ctx.lineWidth=6;ctx.stroke()}\nfunction drawStars(){const fast=worldSpeed()>1;ctx.save();ctx.lineCap='round';state.stars.forEach(s=>{if(fast){ctx.strokeStyle='#d9f3ffaa';ctx.lineWidth=s.s;ctx.beginPath();ctx.moveTo(s.x,s.y);ctx.lineTo(s.x,s.y-30);ctx.stroke()}else{ctx.fillStyle='#ffffffaa';ctx.beginPath();ctx.arc(s.x,s.y,s.s,0,Math.PI*2);ctx.fill()}});ctx.restore()}\nfunction drawPlayer(){const sh=ships[state.ship];const key=sh.id;ctx.save();ctx.translate(player.x,player.y);ctx.globalAlpha=player.inv>0&&Math.floor(player.inv*12)%2?0.55:1;if(state.shieldT>0||player.inv>0){ctx.strokeStyle='#6fe9ffbb';ctx.lineWidth=3;ctx.beginPath();ctx.ellipse(0,0,38,45,0,0,Math.PI*2);ctx.stroke()}ctx.restore();drawSpriteSheet('players',key,player.x,player.y,key==='tank'?86:key==='sting'?82:84,key==='tank'?96:92);ctx.save();ctx.translate(player.x,player.y+26);ctx.fillStyle=state.boost?'#fff27c':'#8bf1ff';ctx.beginPath();ctx.ellipse(0,10,state.boost?8:5,state.boost?28:16,0,0,Math.PI*2);ctx.fill();ctx.restore();if(state.droneT>0){drawSpriteSheet('pickups','drone',player.x+42,player.y-6,34,34,{rot:Math.sin(state.elapsed*6)*0.12})}}\nfunction drawEnemies(){const size={scout:[60,60],zig:[76,58],gun:[78,64],saucer:[84,66]};state.enemies.forEach(e=>{const hp=e.hp/e.max,[w,h]=size[e.type]||[68,60];drawSpriteSheet('enemies',e.type,e.x,e.y,w,h,{rot:e.type==='zig'?Math.sin(e.t*3)*0.05:0});ctx.save();ctx.translate(e.x,e.y);ctx.fillStyle='#0008';ctx.fillRect(-24,-h/2-10,48,5);ctx.fillStyle='#69f0a8';ctx.fillRect(-24,-h/2-10,48*hp,5);ctx.restore()})}\nfunction drawBoss(){const b=state.boss;if(!state.bossActive||!b)return;drawSpriteSheet('bosses',sectors[state.sector].id,b.x,b.y,190,160,{alpha:.98});ctx.save();ctx.translate(b.x,b.y);const mark=(x,y,alive,color)=>{ctx.fillStyle=alive?color:'#1a1633cc';ctx.strokeStyle=alive?'#fff':'#555';ctx.lineWidth=3;ctx.beginPath();ctx.arc(x,y,14,0,Math.PI*2);ctx.fill();ctx.stroke()};mark(-48,8,b.left.hp>0,'#ff8a62');mark(48,8,b.right.hp>0,'#ff8a62');b.coreOpen=b.left.hp<=0&&b.right.hp<=0;mark(0,0,b.coreOpen,b.coreOpen?'#fff06a':'#5ac7ff');ctx.fillStyle='#0009';ctx.fillRect(-70,-88,140,8);ctx.fillStyle='#ff6d8e';ctx.fillRect(-70,-88,140*clamp(b.hp/b.max,0,1),8);ctx.restore()}\nfunction drawBullets(){ctx.fillStyle='#fff069';ctx.shadowColor='#fff069';ctx.shadowBlur=8;state.bullets.forEach(b=>{ctx.beginPath();ctx.arc(b.x,b.y,b.r,0,Math.PI*2);ctx.fill()});ctx.shadowBlur=0;ctx.fillStyle='#ff6dbb';state.enemyBullets.forEach(b=>{ctx.beginPath();ctx.arc(b.x,b.y,b.r,0,Math.PI*2);ctx.fill()})}\nfunction d");
