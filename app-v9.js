(()=>{try{const s=(window.__V9_B64||[]).join('');const bytes=Uint8Array.from(atob(s),c=>c.charCodeAt(0));let code=new TextDecoder().decode(bytes);
code=code.replace("const side = Math.random() < .7 ? 'top' : (Math.random()<.5?'left':'right');","const side = W < 700 ? 'top' : (Math.random() < .78 ? 'top' : (Math.random()<.5?'left':'right'));" );
code=code.replace("p.x = Math.max(34, Math.min(W-34,p.x)); p.y = Math.max(84, Math.min(H-40,p.y));","const safeX = W < 700 ? 62 : 46; const safeBottom = W < 700 ? 150 : 56; p.x = Math.max(safeX, Math.min(W-safeX,p.x)); p.y = Math.max(84, Math.min(H-safeBottom,p.y));");
code=code.replace("if(e.y < e.targetY) e.y += e.speed*dt; else { e.x += Math.sin(e.phase*2.1)*e.strafe*44*dt; e.y += Math.cos(e.phase*1.4)*10*dt; }","if(e.y < e.targetY) e.y += e.speed*dt; else { e.x += Math.sin(e.phase*2.1)*e.strafe*44*dt; e.y += Math.cos(e.phase*1.4)*10*dt; } if(W < 700 && e.y > 20) e.x = Math.max(46, Math.min(W-46,e.x));");
code=code.replace("state.player.x = Math.min(W-50, Math.max(50, state.player.x));\n      state.player.y = Math.min(H-90, Math.max(100, state.player.y));","const mx=W<700?62:50, mb=W<700?150:90; state.player.x = Math.min(W-mx, Math.max(mx, state.player.x)); state.player.y = Math.min(H-mb, Math.max(100, state.player.y));");
code=code.replace("ctx.save(); ctx.globalAlpha=alpha; ctx.translate(x,y); ctx.rotate(rotation); if(img) ctx.drawImage(img,-w/2,-h/2,w,h); else { ctx.fillStyle='#fff'; ctx.fillRect(-w/2,-h/2,w,h);} ctx.restore();","ctx.save(); ctx.globalAlpha=alpha; ctx.translate(x,y); ctx.rotate(rotation); if(img){ ctx.drawImage(img,-w/2,-h/2,w,h); } else if(String(imgKey).startsWith('player')){ ctx.shadowColor='#62eaff'; ctx.shadowBlur=18; ctx.fillStyle='#f4fbff'; ctx.strokeStyle='#58cfff'; ctx.lineWidth=3; ctx.beginPath(); ctx.moveTo(0,-h*.42); ctx.lineTo(w*.33,h*.22); ctx.lineTo(w*.12,h*.16); ctx.lineTo(0,h*.40); ctx.lineTo(-w*.12,h*.16); ctx.lineTo(-w*.33,h*.22); ctx.closePath(); ctx.fill(); ctx.stroke(); ctx.fillStyle='#7de8ff'; ctx.beginPath(); ctx.ellipse(0,-h*.08,w*.10,h*.14,0,0,Math.PI*2); ctx.fill(); } else { ctx.shadowColor='#61e7ff'; ctx.shadowBlur=14; ctx.strokeStyle='#61e7ff'; ctx.lineWidth=3; ctx.fillStyle='rgba(22,54,98,.58)'; ctx.beginPath(); ctx.arc(0,0,Math.min(w,h)*.34,0,Math.PI*2); ctx.fill(); ctx.stroke(); } ctx.restore();");
code=code.replace("drawBackground();\n    const p=state.player;\n    ctx.save(); if(state.shake>0) ctx.translate((Math.random()-.5)*state.shake,(Math.random()-.5)*state.shake);\n    if(!p) return;","drawBackground(); const p=state.player; if(!p) return; ctx.save(); if(state.shake>0) ctx.translate((Math.random()-.5)*state.shake,(Math.random()-.5)*state.shake);");
(0,eval)(code);}catch(e){console.error('V9 boot failed',e);document.body.dataset.v9BootError='1';}})();